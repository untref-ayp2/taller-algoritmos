# Inserción (Insertion Sort)

Construye la lista final ordenada un elemento a la vez, insertando cada nuevo
elemento en su posición correcta entre los ya ordenados. Similar a ordenar
cartas en la mano. Es estable, in-place y online.

```bash
go run main.go
```
