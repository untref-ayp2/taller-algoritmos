# Selección (Selection Sort)

Encuentra repetidamente el elemento mínimo del resto no ordenado y lo coloca
al principio de la lista. Simple de entender, pero siempre O(n²). No es estable.

```bash
go run main.go
```
