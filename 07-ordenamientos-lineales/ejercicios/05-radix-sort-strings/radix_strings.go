package radix

// RadixSortStrings ordena un slice de cadenas de caracteres alfanuméricos
// mayúscula (0-9, A-Z) de longitud fija `length`.
//
// Procesa desde el carácter menos significativo (derecha) al más significativo
// (izquierda) usando Counting Sort como subrutina estable para cada posición.
func RadixSortStrings(arr []string, length int) []string {
	// TODO: implementar Radix Sort para cadenas
	return nil
}
