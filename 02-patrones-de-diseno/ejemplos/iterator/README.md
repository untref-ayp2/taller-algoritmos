# Iterator — Lista Enlazada

Ejemplo del patrón *Iterator* con el estilo clásico GoF. Define los
métodos `Primero()`, `Siguiente()`, `HaySiguiente()` y `Actual()`
para recorrer una `ListaEnlazada` sin exponer su estructura interna.

```bash
go run main.go
```
